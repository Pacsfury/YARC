#ifndef HELPERS
#define HELPERS

#include <iostream>
#include <string>
#include <vector>

namespace Helpers {

    std::vector<std::string> split(const std::string& text, std::string search, int max=0);
    
} //namespace Helpers

#endif
